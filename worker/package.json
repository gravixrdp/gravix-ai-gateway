{
  "name": "gravix-ai-gateway-worker",
  "version": "1.0.0",
  "description": "High-Performance Edge AI Gateway for OpenAI & Anthropic protocols",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.100.0"
  }
}
